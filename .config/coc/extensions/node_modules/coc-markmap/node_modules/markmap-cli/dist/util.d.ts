/// <reference types="node" />
import type { ReadStream } from 'fs';
import { UrlBuilder } from 'markmap-common';
import { IAssets } from 'markmap-lib';
export declare const ASSETS_PREFIX = "/assets/";
export interface IDevelopOptions {
    /**
     * Whether to open the generated markmap in browser
     */
    open: boolean;
    /**
     * Whether to show the default toolbar
     */
    toolbar: boolean;
    /**
     * Whether to inline all assets to make the HTML work offline.
     * Ignored in watching mode.
     */
    offline: boolean;
    /** Port number for the devServer to listen. */
    port?: number;
}
export declare function addToolbar(urlBuilder: UrlBuilder, assets: IAssets): IAssets;
export declare function localProvider(path: string): string;
export declare const rootDirPromise: Promise<string | undefined>;
export declare const assetsDirPromise: Promise<string>;
export declare function createStreamBody(stream: ReadStream): ReadableStream<any>;
