import { CodeChunkData } from './code-chunk-data';
export declare function pandocConvert(text: any, { fileDirectoryPath, projectDirectoryPath, sourceFilePath, filesCache, protocolsWhiteListRegExp, codeChunksData, graphsCache, imageDirectoryPath, pandocMarkdownFlavor, pandocPath, latexEngine, imageMagickPath, mermaidTheme, plantumlJarPath, plantumlServer, onWillTransformMarkdown, onDidTransformMarkdown, }: {
    fileDirectoryPath: string;
    projectDirectoryPath: string;
    sourceFilePath: string;
    filesCache: {
        [key: string]: string;
    };
    protocolsWhiteListRegExp: RegExp;
    codeChunksData: {
        [key: string]: CodeChunkData;
    };
    graphsCache: {
        [key: string]: string;
    };
    imageDirectoryPath: string;
    pandocPath: string;
    imageMagickPath: string;
    mermaidTheme: string;
    plantumlJarPath: string;
    plantumlServer: string;
    pandocMarkdownFlavor: string;
    latexEngine?: string;
    onWillTransformMarkdown?: (markdown: string) => Promise<string>;
    onDidTransformMarkdown?: (markdown: string) => Promise<string>;
}, config?: {}): Promise<string>;
